# find-ata

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run getTokenBalance.ts\
bun run createNewDataAccount.ts
```

This project was created using `bun init` in bun v1.3.8. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.
